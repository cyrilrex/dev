This directory is reserved for styling paragraphs.

We create one partial per paragraph bundle.

Coupling this with theming by view mode, we can create themes very quickly. All
styling for each paragraph bundle will be inherited every where it is used.

Please give partials in this directory meaningful names. For example, if it's
for the Image with Caption paragraph bundle, name is something like
_image-with-caption.scss
