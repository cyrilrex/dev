This directory is reserved for styling individual views.

We create one partial per view. We give each partial a name relevant to the
main class of the view you are styling, for example:

_view-news-page.scss
