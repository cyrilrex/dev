This directory is reserved for creating paragraphs template files.

If you are only writing a template for a field in a paragraph, then that
template should be stored in the fields directory.

This directory is for writing templates for paragraph bundles, not just one
field in a paragraph bundle.
